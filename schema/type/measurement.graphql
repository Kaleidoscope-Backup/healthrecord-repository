
# measurement descrives a single value of device measurement in time series format which 
# is stored internally in TS DB
type Measurement {
    id: String!
    consumerID: String!
    sourceID: String!
    metricType: String!
    unit: String!
    value: Value!
}